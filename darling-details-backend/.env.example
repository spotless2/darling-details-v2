# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=darling_details
DB_USER=postgres
DB_PASSWORD=yourpassword

# Set this to true to force recreate tables (development only)
DB_SYNC_FORCE=false

# JWT Configuration
JWT_SECRET=change-this-in-production
JWT_EXPIRATION=86400
